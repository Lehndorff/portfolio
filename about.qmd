---
title: "About me"
author: "Hans Lehndorff"
date-modified: "today"
about: 
  template: trestles
---

I am currently a consultant at [Evergreen Economics](https://evergreenecon.com).

[Evergreen Economics Resume](https://evergreenecon.com/wp-content/uploads/2021/06/Lehndorff-Resume_June2021.pdf)

I started at Evergreen in 2016 after graduating from Willamette University.

The logo for this website is randomly generated (on occasion).

```{r logo}

library(dplyr)
library(ggplot2)

start<-runif(3)
amount<-10000
margins=.4

grid<-data.frame(xin=runif(amount,-1,1),yin=runif(amount,-1,1),size=runif(amount,-1,1),group=round(runif(amount,1,10))) %>% 
  group_by(group) %>% 
  mutate(
    order=1:n(),
    r=rep(start[1]+runif(1,start[1]*-margins,start[1]*margins),n()),
    g=rep(start[2]+runif(1,start[2]*-margins,start[2]*margins),n()),
    b=rep(start[3]+runif(1,start[3]*-margins,start[3]*margins),n())
    ) %>% 
  ungroup() %>% 
  mutate(across(r:b,function(x){
    x[x<0]<-0
    x[x>1]<-1
    x
  })) %>% 
  mutate(color=rgb(r,g,b)) %>% 
  arrange(order)

plot<-ggplot(grid)+
  geom_rect(xmin=-1.2,xmax=1.2,ymax=1.2,ymin=-1.2,fill="white")+
  geom_path(aes(x=xin,y=yin,group=group,color=color))+
  theme_void()+
  guides(color='none')+
  scale_color_manual(values = unique(grid$color))+
  geom_rect(xmin=-.25,xmax=.25,ymax=1,ymin=.25,fill="white")+
  geom_rect(xmin=-.25,xmax=.25,ymax=-.25,ymin=-1,fill="white")

```

![The Logo](data/logo.png){fig-align="center"}
